#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

#define _ ios::sync_with_stdio(0);cin.tie(nullptr);

// 

int main() { _
    ll n; cin >> n;

    vector<ll> v(n);

    ll mid = n / 2;

    
}